<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <ImageView
        android:id="@+id/iv_show_pic"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/fl_save_pic" />

    <FrameLayout
        android:id="@+id/fl_save_pic"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_gravity="center_vertical"
        android:paddingLeft="10dp"
        android:paddingRight="10dp" >

        <Button
            android:id="@+id/bt_take_pic"
            android:layout_width="150dp"
            android:layout_height="60dp"
            android:background="@drawable/mes_button"
            android:layout_gravity="left"
            android:textColor="@color/mes_header_tv_blue"
            android:text="@string/bt_take_pic" />

        <Button
            android:id="@+id/bt_save_pic"
            android:layout_width="150dp"
            android:layout_height="60dp"
            android:background="@drawable/mes_button"
            android:layout_gravity="right"
            android:textColor="@color/mes_header_tv_blue"
            android:text="@string/bt_save_pic" />
    </FrameLayout>

</RelativeLayout>